---
import BaseHtmlHead from "./BaseHtmlHead.astro";
import TopNavigation from "./NavigationBar";

import config from "config";
const { author } = config;

interface Props {
  title: string;
  description: string;
  fullWidth?: boolean;
}
const { title, description } = Astro.props;

const year = new Date().getFullYear();
---

<html lang="zh">
  <head>
    <BaseHtmlHead title={title} description={description} />
  </head>
  <body
    class="bg-white dark:bg-zinc-900 text-zinc-800 dark:text-zinc-300 h-full"
  >
    <TopNavigation client:load />
    <main class="py-4 px-6 mx-auto max-w-screen-md">
      <slot />
    </main>
  </body><footer
    class="p-4 mx-auto max-w-screen-md text-center text-sm text-zinc-400"
  >
    <p>
      <a href="/changelog" class="underline">Changelog</a>
    </p>
    <p>
      © {year}
      {author}. All rights reserved.
    </p>
  </footer>
</html>
